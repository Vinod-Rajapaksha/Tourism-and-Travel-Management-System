# Database Configuration (SQL Server)
spring.datasource.url=jdbc:sqlserver://localhost:1433;databaseName=TTMS;encrypt=false
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver
spring.datasource.username=sa
spring.datasource.password=1231

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServer2016Dialect
spring.jpa.properties.hibernate.format_sql=true

#2025/09/20

spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# Use H2 instead
#spring.datasource.url=jdbc:h2:mem:testdb
#spring.datasource.driver-class-name=org.h2.Driver
#spring.datasource.username=sa
#spring.datasource.password=1231
#spring.h2.console.enabled=true

# Disable JPA initialization for now
# Add these lines to STOP data insertion
spring.sql.init.mode=never


# Enable detailed logging
logging.level.org.springframework=INFO
logging.level.com.tourguide=DEBUG
logging.level.web=DEBUG

# Show startup info
logging.level.org.springframework.boot=INFO
logging.level.org.apache.tomcat=INFO

# Show SQL queries (if using database)
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Show HikariCP connection pool info
logging.level.com.zaxxer.hikari=DEBUG
# Add these lines:
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always
